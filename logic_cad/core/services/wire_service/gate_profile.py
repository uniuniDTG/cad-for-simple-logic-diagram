"""Routing profile when connecting to AND/OR gate inputs (bundle optimization)."""

from __future__ import annotations

from logic_cad.core.routing import DEFAULT_ROUTING_PROFILE

_GATE_CONNECT_OPTIMIZE_PROFILE = DEFAULT_ROUTING_PROFILE
